{
  "name": "css-in-react",
  "version": "1.0.0",
  "description": "Slides for CSS in React talk",
  "main": "index.js",
  "scripts": {
    "test": "npm test",
    "start": "mdx-deck deck.js",
    "build": "mdx-deck build deck.js --no-html",
    "now-build": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@researchgate/react-intersection-observer": "^0.7.4",
    "intersection-observer": "^0.5.1",
    "prop-types": "^15.7.2"
  },
  "devDependencies": {
    "@now/static-build": "^0.5.0",
    "mdx-deck": "^2.3.2"
  }
}
